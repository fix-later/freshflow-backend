# Báo cáo khảo sát & kế hoạch triển khai: HUB — Hub Operations, Reconciliation & Cross-docking

| | |
|---|---|
| **Ngày khảo sát** | 2026-07-10 |
| **Branch** | tách từ `dev` (đề xuất `SCRUM-256-HUB-Operations`) — module Hub hiện RỖNG |
| **Epic** | SCRUM-256 — Hub Operations, Reconciliation & Cross-docking |
| **Phạm vi** | 6 task **backend** (bỏ 6 task FE-WEB song song 287/289/291/293/295/297). Nằm TRONG module **Hub** (đang RỖNG — bootstrap từ đầu, giống Logistics trước epic LOG). Bao gồm: Manage Hub, Receive/Reconcile Inbound (scan), Discrepancy + refund-qua-credit + notify, Cross-dock, Handover/Driver Checkout, Inbound/Outbound History. |
| **Tác giả** | Leader agent (team `backend-dev`) — CHỈ nghiên cứu + lập plan. Code do "codex" (tool ngoài) implement theo prompt supervisor; "reviewer" review độc lập diff của codex. |
| **Mục đích** | Cung cấp đủ ngữ cảnh + quyết định thiết kế để codex implement không cần hỏi lại. Nguồn tham chiếu chính, KHÔNG chỉ TaskList. |
| **Trạng thái** | Khảo sát xong. Các điểm mở đã được leader tự chốt (DEC-HUB-01…14) theo code THẬT + convention repo. 6 task "chưa bắt đầu", sẵn sàng breakdown cho codex bắt đầu SCRUM-286. |

> **Nguồn Jira:** breakdown 6 task BE do supervisor cung cấp (286/288/290/292/294/296). Scope suy ra từ tiêu đề UC-HUB + **FR-HUB-001..005, FR-HUB-NEW-001..003** (`docs/01-requirements-spec.md` §1.5, dòng 98-109) + DDL `docs/03-database-schema.md` dòng 659-776. UC-HUB-11+12 (SCRUM-294 Handover/Driver Checkout) KHÔNG có FR-HUB riêng — suy ra từ định nghĩa vai trò Hub Staff "hand off to Driver" (`docs/01` dòng 278) + phụ thuộc Logistics `DeliveryRoute.DriverUserId` (đã có từ LOG). Xem DEC-HUB-11.

> **Quy ước cập nhật:** mỗi khi 1 task PASS hoặc có quyết định giữa chừng → cập nhật §0 (bảng trạng thái), §4 (breakdown), §6 (nhật ký). Đồng bộ TaskList + memory `project_hub_epic`, không thay thế.

---

## 0. Bảng SCRUM key theo task

| Thứ tự build | SCRUM Key | UC | Tên | Loại | Trạng thái |
|---|---|---|---|---|---|
| 1 | SCRUM-286 | UC-HUB-01 | Manage Hub | Net-new (**bootstrap module**) | ✅ PASS (reviewer 2026-07-10; 47/47 Hub, full suite 0 regression, coverage 91.6%, format+no-drift sạch) |
| 2 | SCRUM-288 | UC-HUB-02+03+04+05 | Receive & Reconcile Incoming Goods (scan + inbound + inventory) | Net-new (core) | ✅ PASS (reviewer 2026-07-10; 89/89, full suite 0 regression, cov 89.5%) — ⚠️ MEDIUM kg→int drift → CHỐT patch decimal(kg) standalone trước commit (DEC-HUB-04 addendum) |
| 3 | SCRUM-290 | UC-HUB-06+07 | Handle Hub Discrepancy (+refund-qua-credit + notify) | Net-new | ⬜ Chưa bắt đầu (phụ thuộc 288) |
| 4 | SCRUM-292 | UC-HUB-08+09+10 | Cross-dock & Prepare Dispatch (+outbound) | Net-new | ⬜ Chưa bắt đầu (phụ thuộc 288) |
| 5 | SCRUM-294 | UC-HUB-11+12 | Handover Goods & Driver Checkout | Net-new (điểm tích hợp Logistics) | ⬜ Chưa bắt đầu (phụ thuộc 292) |
| 6 | SCRUM-296 | UC-HUB-13 | Hub Inbound / Outbound History | Query tổng hợp | ⬜ Chưa bắt đầu (làm cuối) |

**Quy ước commit:** `feat(hub): SCRUM-XXX <description>` — 1 commit/task, đúng key của task. KHÔNG commit khi chưa được supervisor cấp/xác nhận key (giữ trong working tree — memory `feedback_no_commit_without_jira`).

---

## 1. Hiện trạng repo tại thời điểm khảo sát

### 1.1 Module Hub — RỖNG HOÀN TOÀN
- `find src/Modules/Hub -iname "*.cs"` (ngoài obj/bin) → **0 file**. Chỉ có 3 `.csproj` (Domain/Application/Infrastructure).
- `FreshFlow.API/Program.cs` (dòng 185-191) chain `AddAuth/Catalog/Pricing/Orders/Logistics/Notifications/Media` — **KHÔNG có `AddHubModule()`**. Task 1 (286) BẮT BUỘC thêm.
- **Bootstrap EF (2 chỗ, đã đổi so với thời LOG):**
  1. **Runtime discovery** dùng `EfAssemblyRegistry.Register(Assembly.GetExecutingAssembly())` gọi TRONG `AddHubModule()` (mẫu `AddLogisticsModule` dòng 24). KHÔNG còn sửa danh sách cứng ở runtime.
  2. **Design-time migration** vẫn dùng danh sách cứng `DesignTimeDbContextFactory.ForceLoadModuleAssemblies()` (`src/FreshFlow.Infrastructure.Persistence/DesignTimeDbContextFactory.cs:77`) — hiện list Auth/Catalog/Pricing/Orders/Logistics/Notifications. Task 1 BẮT BUỘC thêm `"FreshFlow.Hub.Infrastructure"`, nếu không `dotnet ef` sẽ không thấy EF config Hub (bài học LOG/NOT).
- Không có `FreshFlow.Hub.UnitTests` — cần tạo project test mới (mẫu `tests/Unit/FreshFlow.Logistics.UnitTests/`), đăng ký vào `.slnx` (memory `project_solution_file_slnx` — dùng `FreshFlow.slnx`, KHÔNG `.sln`).

### 1.2 Bảng Hub trong docs/03 — `[PLANNED]`, chưa có trong DB (dòng 659-776)
DDL roadmap tham chiếu (ta ÁP DỤNG với điều chỉnh cross-module FK — xem DEC-HUB-02):
- `hubs` (659) — id, name, address, latitude/longitude NUMERIC(9,6), capacity_kg NUMERIC(10,2) CHECK>0, is_active BOOL, managed_by (docs khai FK users → **ta bỏ FK**), created/updated/deleted_at.
- `hub_inventory` (682) — running stock per (hub_id, market_product_id) UNIQUE; quantity_in/quantity_out INTEGER cumulative, quantity_available = GENERATED (in - out) STORED, CHECK(in>=out). FK hub + market_product → **ta bỏ FK cross-module tới market_products; giữ FK nội bộ hub_id**.
- `hub_inbound_events` (708) — append-heavy; hub_id, source_market_id, delivery_route_id, items JSONB, total_quantity_kg, arrived_at, recorded_by, hub_staff_user_id, condition_status VARCHAR CHECK(OK/DAMAGED/MISSING/PARTIAL), discrepancy_notes.
- `hub_outbound_events` (738) — hub_id, destination_route_id, items JSONB, total_quantity_kg, dispatched_at, recorded_by.
- `cross_dock_transfers` (759) — hub_id, inbound_event_id, outbound_route_id, status TEXT CHECK(pending/in_progress/completed), notes, deleted_at.
- **KHÔNG có** bảng discrepancy chi tiết trong docs/03 (chỉ có `condition_status` trên inbound event). FR-HUB-NEW-002 cần discrepancy record cấp order-item → ta tạo **bảng mới `hub_discrepancies`** (DEC-HUB-05).
- **KHÔNG có** bảng handover trong docs/03. UC-HUB-11+12 → ta tạo **bảng mới `hub_handover_events`** (DEC-HUB-11).
- ⚠️ `payments` & `refunds` = `[PLANNED]` (docs/03 dòng 784/809) — **CHƯA từng implement, KHÔNG có trong DB**. FR-HUB-NEW-003 nói "refund qua payment gateway / bảng refunds" là **SAI so với hệ thống thật** → override bằng credit (DEC-HUB-06).

### 1.3 Đầu vào từ module khác (as-is — đã verify code THẬT)
- **Credit/công nợ (Orders module, epic CRE đã xong):** `ICreditService.RefundAsync(restaurantId, orderId, amount, note, ct)` (`Orders.Application/Abstractions/ICreditService.cs` + impl `Services/CreditService.cs:62`) → tạo `CreditTransaction(type=Refund)` GIẢM `OutstandingBalance`, guard `amount <= OutstandingBalance` (nếu vượt → `CREDIT_REFUND_EXCEEDS_BALANCE`). **Đây là cơ chế "hoàn tiền" thật của hệ thống** — KHÔNG có payment gateway per-order. Restaurant thanh toán qua công nợ (memory `project_payment_model`, `project_cre_epic`).
- **Giá khoá (Orders):** `OrderItem.LockedUnitPrice` (`Orders.Domain/Entities/OrderItem.cs:26`) set lúc confirm order (FR-ORD-008 AC2). `LockedTotal = Quantity × LockedUnitPrice`. → refund amount = `affectedQuantity × LockedUnitPrice`. **Orders sở hữu dữ liệu giá này** → Orders là nơi tính refund (DEC-HUB-06).
- **OrderItem** = `Id, OrderId, MarketProductId, ProductNameSnapshot, Quantity, UnitPrice, LockedUnitPrice?, LockedTotal?, ActualQuantity?`. Có `RecordActualQuantity()` (comment: "may be less than ordered if Hub Staff flags a shortage") — hook đã chờ sẵn cho Hub.
- **OrderIssue (Orders, đã có):** `OrderIssue(orderId, orderItemId?, reportedBy, IssueType{Missing/Wrong/Damaged}, affectedQuantity, description)` + `Status{Open/Resolved}` + `Resolve()`. GẦN GIỐNG discrepancy nhưng là **order-scoped, Orders-owned**, KHÔNG gắn hub/inbound. → KHÔNG tái dùng làm bảng discrepancy Hub (DEC-HUB-05), nhưng có thể là điểm nối "block DELIVERING" (DEC-HUB-07).
- **Order status (Orders):** enum `Draft/Confirmed/Batched/PickedUp/AtHub/Delivering/Delivered/Cancelled`. `AdvanceStatus(next)` với transition map có `AtHub → Delivering` (`Order.cs:17`). **Command trigger `Delivering` thuộc DEL epic (SCRUM-299) CHƯA build** → điểm enforce "block DELIVERING khi có discrepancy" defer/nối DEL (DEC-HUB-07).
- **Logistics (epic LOG đã xong):** `DeliveryRoute` có `Id, RouteType, Status(planned/selected/reviewed/assigned/cancelled), ServiceDate, Stops(JSONB), VehicleId?, DriverUserId?, OrderGroupId?, ...`. `Vehicle`, `DeliveryZone` đã có. Cross-dock `outboundRouteId`/inbound `deliveryRouteId`/handover `deliveryRouteId` = tham chiếu `DeliveryRoute.Id` qua **Guid + read-seam**, KHÔNG FK chéo module (DEC-HUB-02).
- **Catalog:** `market_products` (cột `hub_inventory.market_product_id` trỏ tới) + `Market` có `Latitude/Longitude`. Đọc qua read-seam nếu cần validate product/market tồn tại.
- **Auth:** role `hub_staff` seed sẵn (migration `20260606152229_AddRolesTable.cs:53`, string `"hub_staff"`); `operations_manager`, `admin`, `driver` cũng có. `DriverProfile`/`users`→`roles` đọc qua seam (mẫu Logistics `DriverReader`).

### 1.4 Cross-module read-seam pattern (TÁI DÙNG — KHÔNG viết mới)
`Logistics.Infrastructure/CrossModule/` là mẫu chuẩn mới nhất:
- `XxxRow` (POCO), `XxxRowConfiguration : IEntityTypeConfiguration<XxxRow>` với `builder.HasNoKey(); builder.ToSqlQuery("""SELECT ... FROM <other_module_table> WHERE ...""")` — **KHÔNG `ToTable`** (tránh model-conflict với module chủ). `XxxReader(AppDbContext db) : IXxxReader` → `db.Set<XxxRow>().AsNoTracking().FirstOrDefaultAsync(...)`. Interface ở `*.Application/Abstractions`, impl `internal sealed` ở `*.Infrastructure/CrossModule`, đăng ký DI trong `AddHubModule`.
- **CASING GOTCHA:** bảng module khác (Auth/Catalog/Orders/Logistics) = **PascalCase column** → SQL phải quote `"Id"`,`"LockedUnitPrice"`,`"DeletedAt"`... Bảng Hub tự sở hữu = **snake_case** (DEC-HUB-03). 2 convention coexist (memory `project_ef_column_casing`).

### 1.5 SignalR / realtime — as-is
- `Program.cs` map CHỈ `PricingHub` (`/hubs/pricing`) + `OrderHub` (`/hubs/orders`). **KHÔNG có `DeliveryHub`** dù CLAUDE.md nhắc. Notifications module KHÔNG có `IHubContext`/live broadcast — chỉ `INotificationWriter.WriteAsync(...)` **persist notification vào DB** (mẫu `OrderConfirmedIntegrationEventHandler`). → realtime <500ms của FR-HUB-NEW-003 AC2 **defer thành DB-persist** (DEC-HUB-08), nhất quán NOT epic (memory `project_not_epic`: hub/delivery realtime blocked-by epic của chúng).

### 1.6 Integration events hiện có (`src/Shared/FreshFlow.Contracts/`)
`OrderConfirmedIntegrationEvent`, `OrderCancelledIntegrationEvent`, `CreditLimitThresholdReachedIntegrationEvent` (đều `record ... : INotification`). Consumer = `INotificationHandler<TEvent>` trong `*.Application/EventHandlers/`. Hub sẽ THÊM event mới vào Contracts (DEC-HUB-06/DEC-HUB-09).

### 1.7 Roles & RBAC
`[Authorize(Roles="...")]` string-based mức class controller. Quy ước:
- **Admin/quản trị Hub** (FR-HUB-001 tạo/sửa/xoá hub, acknowledge discrepancy): `admin,operations_manager`.
- **Vận hành hàng ngày** (scan, inbound, discrepancy-create, cross-dock, outbound, handover): `hub_staff` (+ `admin,operations_manager` được phép). "Hub Staff can only record inbound/discrepancy events for their hub" (docs/01 dòng 167) — isolation theo hub (DEC-HUB-13).

---

## 2. Quyết định thiết kế (DEC-HUB) — leader tự chốt theo code THẬT + convention

- **DEC-HUB-01 — Bootstrap ở SCRUM-286.** Task 1 tạo: `AddHubModule(config)` (gọi `EfAssemblyRegistry.Register` + `AddMediatR` + `AddValidatorsFromAssembly` + `ValidationBehavior` mẫu `AddLogisticsModule`); chain `.AddHubModule()` vào `Program.cs`; thêm `"FreshFlow.Hub.Infrastructure"` vào `ForceLoadModuleAssemblies`; tạo `tests/Unit/FreshFlow.Hub.UnitTests` + đăng ký `.slnx`. Config `Hub:*` (vd default nếu cần) đặt sẵn.
- **DEC-HUB-02 — KHÔNG cross-module FK; chỉ `Guid` + index.** Ghi đè DDL docs/03. `hubs.managed_by`(→users), `hub_inventory.market_product_id`(→market_products), `hub_inbound_events.source_market_id/delivery_route_id/recorded_by/hub_staff_user_id`, `hub_outbound_events.destination_route_id`, `cross_dock_transfers.outbound_route_id`, `hub_discrepancies.order_id/order_item_id/restaurant_id` = **Guid trần + index**, KHÔNG `HasForeignKey` sang bảng module khác. GIỮ FK nội-bộ-Hub (`hub_inventory.hub_id`→`hubs`, `hub_inbound_events.hub_id`→`hubs`, `cross_dock_transfers.inbound_event_id`→`hub_inbound_events`, `hub_discrepancies.inbound_event_id`→`hub_inbound_events`). Validate tồn tại cross-module qua read-seam (id không tồn tại → 404/422 rõ ràng).
- **DEC-HUB-03 — Column casing = snake_case tường minh (`HasColumnName`).** Nhất quán module Logistics/Orders/Notifications (memory `project_ef_column_casing`). Bảng Hub tự sở hữu: `ToTable("hubs")`, `HasColumnName("capacity_kg")`,... Filtered unique index `HasFilter("deleted_at IS NULL")` cho mutable table. KHÔNG mirror PascalCase-mặc-định của Catalog.
- **DEC-HUB-04 — `occupiedCapacityKg` = tính từ `hub_inventory`, KHÔNG cột vật lý trên `hubs`.** docs/03 KHÔNG có cột `occupied_capacity_kg` trên `hubs`; `hub_inventory.quantity_available` (GENERATED) là nguồn chân lý tồn kho. `occupiedCapacityKg` (FR-HUB-001 AC2, NEW-001 AC4) = tổng trọng lượng tồn hiện tại. **Đơn vị:** inbound/outbound items dùng `quantityKg` (FR-HUB-003/004) nhưng `hub_inventory` là INTEGER quantity per market_product. **Quyết định MVP:** track `occupied_capacity_kg` như **cột NUMERIC trên `hubs`, cập nhật atomic** cùng inbound(+)/outbound(−) trong 1 transaction (đơn giản, đúng AC "updated atomically"); `hub_inventory` giữ đếm quantity per product cho stock-check outbound. `availableCapacityKg = capacity_kg − occupied_capacity_kg`. **CẬP NHẬT (chốt 2026-07-10): thống nhất TẤT CẢ đo bằng kg** — `hub_inventory.quantity_in/out/available` đổi sang `decimal NUMERIC(12,2)` (xem addendum), nên `occupied_capacity_kg` và stock-per-product cùng đơn vị kg; KHÔNG còn "2 chiều đo song song".
  - **⚠️ ADDENDUM — CHỐT (supervisor 2026-07-10, option (a)):** code 288 dùng `decimal.ToInt32(item.QuantityKg)` (round-per-item rồi cộng) vì `hub_inventory.quantity_in/out` là INTEGER → **drift thật, không chỉ mất precision:** (1) round trước khi sum trong 1 event (2×0.4kg→0+0=0 thay vì 0.8→1; domain cho phép trùng market_product); (2) cộng dồn qua nhiều event (10×0.6kg→10 thay vì 6); (3) 292 `INSUFFICIENT_HUB_STOCK` guard đọc `quantity_in` sai → chặn nhầm/over-dispatch. **Leader ĐỀ XUẤT: option (a) — đổi `HubInventory.QuantityIn/QuantityOut/QuantityAvailable` sang `decimal` (NUMERIC(12,2) kg)**, thống nhất mọi thứ đo bằng kg (khớp inbound/outbound/capacity), bỏ conversion lossy; contained trong entity + EF config + migration column type. **Override docs/03 INTEGER** (đã override docs/03 nhiều lần: FK, occupied col). Migration: vì KHÔNG apply DB thật → sửa thẳng column type trong migration `AddHubInboundAndInventory` của 288 nếu chưa push, hoặc alter-migration ở fix round nếu đã commit. THỰC THI (chốt): standalone patch codex NGAY (286+288 chưa commit) — sửa thẳng entity + EF config + migration `AddHubInboundAndInventory` + bỏ `decimal.ToInt32` trong ScanInboundCommandHandler; gộp luôn 2 LOW (test `GetPendingInboundQueryValidator`/`ListInboundQueryValidator`). KHÔNG fold 290/292. reviewer re-review nhanh phần đổi kiểu. (Option (b) giữ int đã bị loại — vẫn drift cross-event.)
- **DEC-HUB-05 — Bảng MỚI `hub_discrepancies` (KHÔNG tái dùng Orders.OrderIssue).** OrderIssue là Orders-owned, order-scoped, không gắn hub/inbound. Discrepancy Hub cần `inbound_event_id` (FK nội bộ) + `hub_id`. Schema: `id UUID PK, hub_id UUID (FK hubs), inbound_event_id UUID (FK hub_inbound_events), order_id UUID, order_item_id UUID, restaurant_id UUID, affected_quantity NUMERIC(10,2) CHECK>0, condition_status VARCHAR(20) CHECK(MISSING/DAMAGED/PARTIAL), notes TEXT, status VARCHAR(20) CHECK(OPEN/ACKNOWLEDGED) DEFAULT OPEN, acknowledged_by UUID?, acknowledged_at TIMESTAMPTZ?, created_at, updated_at, deleted_at`. Aggregate `HubDiscrepancy` với `Acknowledge(adminUserId)` (guard OPEN→ACKNOWLEDGED). Lý do đặt ở Hub: endpoint hub-scoped `/api/hub/{hubId}/inbound/{inboundId}/discrepancy`, isolation theo hub.
- **DEC-HUB-06 — FR-HUB-NEW-003 refund = CREDIT ADJUSTMENT qua `ICreditService.RefundAsync`, KHÔNG payment gateway. (OVERRIDE docs.)** Bảng `refunds`/`payments` KHÔNG tồn tại; hệ thống B2B công nợ. **Luồng:** Hub ghi discrepancy → raise domain event → publish `HubDiscrepancyRecordedIntegrationEvent(DiscrepancyId, HubId, InboundEventId, OrderId, OrderItemId, AffectedQuantity, ConditionStatus, OccurredAt)` vào Contracts → **Orders.Application** consume (`INotificationHandler`) → resolve `OrderItem.LockedUnitPrice` + `Order.RestaurantId` (Orders sở hữu) → `refundAmount = AffectedQuantity × LockedUnitPrice` → `ICreditService.RefundAsync(restaurantId, orderId, refundAmount, note)` (credit note giảm công nợ). Orders KHÔNG được cấu hình refund vượt balance → nếu `refundAmount > OutstandingBalance` thì clamp về balance HOẶC log-warn skip (chốt: **clamp về `min(refundAmount, OutstandingBalance)`**, ghi note; 0 balance → skip tạo transaction, vẫn notify). Idempotency: dùng `DiscrepancyId` làm khoá (Orders check đã refund discrepancy này chưa — lưu `credit_transactions.note`/ref hoặc bảng phụ; MVP đơn giản = check tồn tại transaction ref=DiscrepancyId). Retry/backoff 3 lần (AC4) = DEFER (in-process handler; nếu fail chỉ log — ghi rõ simplification). **Đây là DEC quan trọng nhất — codex PHẢI theo, KHÔNG gọi "payment gateway".**
- **DEC-HUB-07 — "Block DELIVERING khi có discrepancy mở" (NEW-002 AC2) = enforce hook DEFER sang DEL epic; Hub cung cấp read-seam.** Command trigger `Order.AdvanceStatus(Delivering)` thuộc DEL epic (SCRUM-299) CHƯA build. Hub KHÔNG sửa Orders state machine (tránh reverse-coupling + entity không async-lookup được). **Quyết định:** Hub epic build đầy đủ discrepancy CRUD + acknowledge + `GET .../discrepancies?status=OPEN`, và **expose `IHubDiscrepancyReader.HasOpenDiscrepanciesForOrderAsync(orderId)`** (read-seam để DEL/Orders tra khi build transition→Delivering, trả 409 `PENDING_HUB_DISCREPANCY`). Ghi rõ: enforcement point là DEL-epic follow-up (giống LOG defer HUB_RELAY). Nếu supervisor muốn enforce NGAY, cần mở scope sang Orders/DEL — flag ra ngoài epic.
- **DEC-HUB-08 — Realtime notify (NEW-003 AC2 `HubDiscrepancyNotification` <500ms SignalR) = DB-persist qua Notifications, SignalR live DEFER.** Notifications module hiện chỉ persist DB (`INotificationWriter`), không map DeliveryHub. **Luồng:** Orders (sau khi tính refund) publish `RestaurantRefundIssuedIntegrationEvent(RestaurantId, OrderId, OrderItemName, AffectedQuantity, RefundAmount, OccurredAt)` → Notifications consume → `WriteAsync(userId, notification_type, title, body, payload)` persist DB. Lý do publish event thứ 2 (thay vì Notifications consume trực tiếp `HubDiscrepancyRecordedIntegrationEvent`): Notifications cần `restaurantId + itemName + refundAmount` mà chỉ Orders resolve được từ `OrderItem`. Live SignalR broadcast = follow-up khi DeliveryHub/backplane sẵn sàng (nhất quán NOT epic).
- **DEC-HUB-09 — Integration events MỚI thêm vào Contracts:** `HubDiscrepancyRecordedIntegrationEvent` (Hub→Orders), `RestaurantRefundIssuedIntegrationEvent` (Orders→Notifications). Đặt ở `src/Shared/FreshFlow.Contracts/`, `record ... : INotification`. Hub raise domain event `HubDiscrepancyRecordedDomainEvent` trên aggregate → handler nội-bộ-Hub publish integration event (mẫu Orders `OrderConfirmedDomainEventHandler`).
- **DEC-HUB-10 — Inventory & capacity cập nhật ATOMIC trong transaction.** Inbound: `hub_inventory.quantity_in += qty` (upsert theo (hub,market_product)), `hubs.occupied_capacity_kg += totalKg`, tạo `hub_inbound_events`, guard `occupied + totalKg <= capacity_kg` else **422 `HUB_CAPACITY_EXCEEDED`**. Trùng `delivery_schedule_id` đã inbound-confirmed → **409 `ALREADY_RECEIVED`** (unique index trên (hub_id, delivery_schedule_id) hoặc check tồn tại). Outbound: guard `quantity_out + qty <= quantity_in` per product else **422 `INSUFFICIENT_HUB_STOCK`**, `quantity_out += qty`, `occupied_capacity_kg -= totalKg`. Dùng optimistic concurrency (`UpdatedAt`/xVersion) hoặc row-lock để chống race (mẫu CRE credit). `quantity_available` là GENERATED column — KHÔNG ghi trực tiếp.
- **DEC-HUB-11 — SCRUM-294 Handover/Driver Checkout = bảng MỚI `hub_handover_events`, self-contained Hub, đọc DeliveryRoute/driver qua seam.** Không có FR-HUB riêng. Model: hub staff bàn giao goods (outbound/cross-dock đã chuẩn bị) cho driver của 1 route; driver "checkout" xác nhận nhận đủ trước khi rời hub. Schema: `id UUID PK, hub_id UUID (FK hubs), delivery_route_id UUID, driver_user_id UUID, outbound_event_id UUID?, status VARCHAR(20) CHECK(PENDING_CHECKOUT/CHECKED_OUT), handed_over_by UUID, handed_over_at TIMESTAMPTZ, driver_confirmed_at TIMESTAMPTZ?, notes, created_at, updated_at, deleted_at`. Checkout guard: **route KHÔNG được có discrepancy mở** (dùng `IHubDiscrepancyReader` — DEC-HUB-07) → nếu có → 409 `PENDING_HUB_DISCREPANCY`; verify `driver_user_id` khớp `DeliveryRoute.DriverUserId` (seam) + role driver. **DEC-HUB-11 phụ thuộc 292** (outbound đã có). Phần "driver bắt đầu giao / Delivering" thật sự thuộc DEL epic — Hub chỉ ghi nhận handover tại hub. Nếu supervisor thấy 294 nên gộp vào DEL epic, có thể defer — flag để supervisor quyết.
- **DEC-HUB-12 — Migration: 1 migration/task, create-only, KHÔNG apply DB thật, KHÔNG commit khi chưa có key.** Sau mỗi migration chạy `dotnet ef migrations has-pending-model-changes` = no drift. `condition_status`/`status` = **VARCHAR + CHECK constraint** (mẫu docs/03 dùng CHECK, KHÔNG PG enum cho các cột này — khớp `cross_dock_transfers.status` docs/03 dòng 758 "not a PG enum to allow simpler additions"). `quantity_available GENERATED ALWAYS AS (...) STORED` khai qua `HasComputedColumnSql(..., stored: true)`. Chạy `dotnet build-server shutdown` sau mỗi lệnh dotnet one-shot (memory `feedback_dotnet_cleanup`).
- **DEC-HUB-13 — RBAC & hub-isolation.** Quản trị hub (286 create/update/deactivate) + acknowledge discrepancy (290) = `admin,operations_manager`. Vận hành (scan/inbound 288, discrepancy-create 290, cross-dock/outbound 292, handover 294) = `hub_staff,admin,operations_manager`. History (296) = `admin,operations_manager,hub_staff`. MVP: check role; hub-scoped isolation ("hub staff chỉ thao tác hub của mình") — DriverProfile/hub assignment CHƯA có bảng gán hub-staff↔hub → **isolation theo hub_id trong path** (mọi thao tác gắn hubId rõ ràng, hub tồn tại + active); gán nhân sự↔hub = follow-up ngoài epic (ghi rõ).
- **DEC-HUB-14 — API namespace.** docs/01 dùng lẫn `/api/hub/...`, `/api/hubs/...`, `/api/admin/hubs`. Repo convention thật = `/api/v1/...` (mọi controller). **Chuẩn hoá:** `/api/v1/hubs` (quản trị + inbound/outbound/cross-dock/discrepancy/handover đều nằm dưới), giữ shape path AC (`/{hubId}/inbound`, `/{hubId}/inbound/{inboundId}/discrepancy`, `/{hubId}/cross-dock`, `/{hubId}/outbound`, `/{hubId}/pending-inbound`, `/{hubId}/discrepancies`, `/scan`). Ghi rõ prefix `/api/v1/`.

---
- **DEC-HUB-15 (leader chốt 2026-07-10, verify code thật 288) — 290 giới thiệu Hub→Orders read-seam đầu tiên để resolve+validate `orderItemId → orderId` lúc tạo discrepancy.** `HubInboundItem` KHÔNG mang orderItemId (chỉ market_product/kg) và AC1 request chỉ có `orderItemId` (không orderId), nhưng discrepancy cần `order_id` (cho reader `HasOpenDiscrepanciesForOrderAsync` mà DEL sẽ dùng) + cần validate orderItem thật (an toàn tài chính vì kích hoạt refund). ⇒ seam `IOrderLookupReader` (`HasNoKey`+`ToSqlQuery` trên `order_items`, **PascalCase columns** — order_items dùng EF default, xem correction memory `project_ef_column_casing`), không thấy → 404 `ORDER_ITEM_NOT_FOUND`. Hub lưu `order_id`+`order_item_id`, KHÔNG lưu restaurant_id/lockedUnitPrice (Orders resolve khi refund/notify — phân vai rõ với DEC-HUB-06). **`HubDiscrepancy` là entity Hub DUY NHẤT kế thừa `AggregateRoot`** (bắt buộc để `DomainEventDispatchInterceptor` dispatch domain event; entity Hub khác là plain class).

## 3. Rủi ro & bề mặt bảo mật

- **RBAC:** đúng DEC-HUB-13. KHÔNG lộ thao tác hub cho `restaurant`/`driver` (trừ driver checkout của chính route mình — verify driverUserId khớp route qua seam, chống IDOR).
- **Input validation (FluentValidation co-located):** capacity_kg > 0; lat/lng ±90/±180; items non-empty, quantityKg > 0; conditionStatus ∈ enum; affectedQuantity > 0; scan payload non-empty. Vượt capacity → 422; over-dispatch → 422; trùng inbound → 409; deactivate hub có pending inbound → 409 `HUB_HAS_PENDING_DELIVERIES`; scan no-match → 404 `SCAN_NO_MATCH`; cross-dock sai inbound status/hub mismatch → 422.
- **IDOR / cross-module integrity:** validate hubId/orderItemId/routeId/marketId tồn tại qua read-seam trước khi ghi; KHÔNG tin id client. Driver checkout: driverUserId phải = `DeliveryRoute.DriverUserId`.
- **Concurrency (DEC-HUB-10):** inbound/outbound cập nhật inventory+capacity phải atomic + optimistic token / row-lock; 2 scan cùng lúc → 1 thắng, tránh double-count. `ALREADY_RECEIVED` chống double-inbound bằng unique index.
- **Refund an toàn (DEC-HUB-06):** clamp refund ≤ outstanding balance; idempotent theo DiscrepancyId (không refund 2 lần khi event re-deliver). KHÔNG lộ số liệu công nợ ra ngoài restaurant sở hữu.
- **Timezone:** business date (history `?date=`, arrived_at/dispatched_at) theo `Asia/Ho_Chi_Minh` cho filter; lưu UTC (mẫu Orders).
- **Migration an toàn:** create-only, no-drift, KHÔNG apply DB thật, KHÔNG commit khi chưa có key. Secrets: không hardcode; không secret mới.

---

## 4. Phân rã task (theo thứ tự build)

### SCRUM-286 — Manage Hub (UC-HUB-01) — *bootstrap module*
**Mục tiêu:** CRUD Hub registry + khởi tạo hạ tầng module Hub. FR-HUB-001.
- **Domain:** `Hub` aggregate (`Id, Name, Address?, Latitude?, Longitude?, CapacityKg, OccupiedCapacityKg (default 0), IsActive, ManagedBy?, timestamps, DeletedAt`). Methods: `Create(...)` guard CapacityKg>0 + lat/lng range; `Update(...)`; `Deactivate()` (guard: KHÔNG có pending inbound — nhưng pending inbound check nằm ở handler vì cần query event; ở 286 chưa có bảng inbound → **286 chỉ set IsActive=false; guard `HUB_HAS_PENDING_DELIVERIES` để 288 bổ sung** hoặc handler 286 để TODO-hook). Prop `AvailableCapacityKg => CapacityKg - OccupiedCapacityKg` (computed, không map).
- **Application:** `CreateHubCommand`+Validator+Handler (→201 + hubId), `UpdateHubCommand` (PATCH mọi field trừ id →200), `DeactivateHubCommand`, `GetHubQuery`, `ListHubsQuery` (trả occupied/available capacity — FR-HUB-001 AC2). `IHubRepository`. DTO record snake_case (`hub_id`, `capacity_kg`, `occupied_capacity_kg`, `available_capacity_kg`, `is_active`).
- **Infrastructure:** `HubConfiguration` (snake_case `HasColumnName`, `ToTable("hubs")`, KHÔNG FK `managed_by`, filtered index nếu cần), `HubRepository`, `AddHubModule(config)` DI extension (mẫu `AddLogisticsModule`). Migration `AddHubs` (create-only, `capacity_kg` CHECK>0, `occupied_capacity_kg` NUMERIC default 0).
- **Bootstrap (chỉ 1 lần ở task này):** `EfAssemblyRegistry.Register` trong `AddHubModule`; `.AddHubModule(builder.Configuration)` vào `Program.cs` (sau `AddLogisticsModule`); thêm `"FreshFlow.Hub.Infrastructure"` vào `ForceLoadModuleAssemblies`; tạo `tests/Unit/FreshFlow.Hub.UnitTests` + đăng ký `.slnx`.
- **API:** `HubsController` `[Authorize(Roles="admin,operations_manager")]`, `POST/GET/GET{id}/PATCH/DELETE /api/v1/hubs`. Create→201+hubId (AC1); List trả occupied/available (AC2); PATCH→200 (AC3); Deactivate có pending inbound→409 `HUB_HAS_PENDING_DELIVERIES` (AC4 — hook, hoàn thiện ở 288).
- **Test bắt buộc ≥80%:** domain guard (capacity>0, lat/lng range), CRUD handlers, ListHubs trả capacity fields, EF config map cột đúng, validator (capacity≤0, lat/lng ngoài range, name empty), deactivate set inactive. RBAC (admin/ops only).

### SCRUM-288 — Receive & Reconcile Incoming Goods (UC-HUB-02+03+04+05)
**Mục tiêu:** scan nhận hàng + inbound tracking + cập nhật inventory/capacity atomic. FR-HUB-NEW-001, FR-HUB-003.
- **PREREQUISITE FIX (gộp đầu prompt 288, từ review 286 — supervisor chốt):** (a) thêm `ManagedBy` vào `UpdateHubCommand` + handler + validator (đúng FR-HUB-001 AC3 'any field except hubId' — 286 đã cố ý loại, nay sửa) + test; (b) thêm test cho `ListHubsQueryValidator` (PageSize 1-200). KHÔNG tách round-trip codex riêng.
- **Domain:** `HubInboundEvent` aggregate (`Id, HubId, SourceMarketId?, DeliveryRouteId?, DeliveryScheduleId?, Items (JSONB: productId/marketProductId, quantityKg), TotalQuantityKg, ArrivedAt, RecordedBy?, HubStaffUserId?, Status (lifecycle: PENDING→ARRIVED_AT_HUB), ConditionStatus (hàng: OK default), DiscrepancyNotes?, timestamps`). **`Status` TÁCH RIÊNG khỏi `ConditionStatus`** (Status=vòng đời, ConditionStatus=tình trạng hàng). Luồng (CHỐT supervisor): `record-inbound` tạo event **PENDING** — CHƯA động inventory/occupied; `scan` (QR = `HubInboundEvent.Id`) chuyển **PENDING→ARRIVED_AT_HUB** và ĐÂY MỚI là lúc áp dụng inventory (`quantity_in +=`) + `hub.ApplyInbound(totalKg)` atomic. `HubInventory` entity (per hub+market_product, quantity_in/out cumulative, quantity_available GENERATED). `Hub.ApplyInbound(totalKg)` chỉ cộng occupied; guard capacity trong HANDLER (available < totalKg → 422 `HUB_CAPACITY_EXCEEDED`).
- **Application (CHỐT luồng 2 bước):** `RecordInboundCommand(hubId, sourceMarketId?, deliveryScheduleId?, items[], arrivedAt)`+Validator+Handler → tạo inbound event **Status=PENDING** (CHƯA động inventory/occupied); trùng deliveryScheduleId đã tồn tại → 409 `ALREADY_RECEIVED`; trả 201 + inboundId (QR = chính inboundId này). `ScanInboundCommand(code)` (code = `HubInboundEvent.Id` Guid; KHÔNG hỗ trợ deliveryScheduleId)+Handler → parse Guid + tìm event **PENDING đúng hub**; không parse được / không thấy → 404 `SCAN_NO_MATCH`; nếu `hub.AvailableCapacityKg < totalKg` → 422 `HUB_CAPACITY_EXCEEDED`; ATOMIC (1 SaveChanges): Status PENDING→ARRIVED_AT_HUB + upsert inventory `quantity_in +=` per product + `hub.ApplyInbound(totalKg)`; trả 200 + inboundEventId + items. `GetPendingInboundQuery(hubId)` → events `Status IN (PENDING, ARRIVED_AT_HUB)` (AC3 nguyên văn). `ListInboundQuery(hubId, date?)` history + totals. `IHubInboundRepository`, `IHubInventoryRepository`. **Đóng vòng `HasPendingInboundAsync`:** implement THẲNG trong `HubRepository` (query `hub_inbound_events` Status=PENDING cùng AppDbContext), GIỮ chữ ký `IHubRepository`, KHÔNG sửa `DeactivateHubCommandHandler`. **Cross-module validate = DEFER** (MVP lưu id thô, không read-seam ở 288).
- **Infrastructure:** configs snake_case; `HubInventoryConfiguration` với `quantity_available` `HasComputedColumnSql("quantity_in - quantity_out", stored:true)` + unique (hub_id, market_product_id); unique index (hub_id, delivery_schedule_id) chống double-inbound. Migration `AddHubInboundAndInventory`.
- **API:** `POST /api/v1/hubs/{hubId}/inbound` (201+inboundId), `POST /api/v1/hubs/scan` (200/404), `GET /api/v1/hubs/{hubId}/pending-inbound`, `GET /api/v1/hubs/{hubId}/inbound?date=` (AC3 totals). RBAC `hub_staff,admin,operations_manager`.
- **Test ≥80%:** record-inbound→201 tạo PENDING, KHÔNG động inventory/occupied; scan→200 chuyển ARRIVED_AT_HUB + inventory quantity_in tăng + occupied tăng (atomic); scan lúc capacity vượt→422 `HUB_CAPACITY_EXCEEDED`; record trùng deliveryScheduleId→409 `ALREADY_RECEIVED`; scan Guid sai/không parse/không thấy event PENDING→404 `SCAN_NO_MATCH`; pending-inbound list gồm cả PENDING+ARRIVED_AT_HUB; concurrency 2 scan cùng event→1 thắng, không double-count; `HasPendingInboundAsync` thật (có PENDING→true); deactivate hub có pending→409.

### SCRUM-290 — Handle Hub Discrepancy (UC-HUB-06+07) — *phụ thuộc 288*
**Mục tiêu:** flag discrepancy order-item + refund-qua-credit + notify + acknowledge. FR-HUB-NEW-002, FR-HUB-NEW-003. **Task ĐẦU TIÊN của Hub cần domain event → integration event.**

**⚠️ ĐỐI CHIẾU CODE THẬT 288 (bắt buộc theo):**
- **`HubDiscrepancy` PHẢI kế thừa `AggregateRoot`** (`FreshFlow.SharedKernel.Domain`) — KHÁC các entity Hub khác (Hub/HubInboundEvent/HubInventory là plain class). Lý do: `DomainEventDispatchInterceptor` CHỈ quét `ChangeTracker.Entries<AggregateRoot>()` để dispatch domain event post-commit. Plain class KHÔNG được dispatch. `AggregateRoot` cấp sẵn `Id, CreatedAt, UpdatedAt, DeletedAt` (từ `BaseEntity`) + `RaiseDomainEvent()/DomainEvents/ClearDomainEvents()` → EF config theo mẫu Orders (BaseEntity), KHÔNG map Id/timestamps thủ công như Hub.
- **Pattern domain→integration event (mẫu THẬT `Order`/`OrderConfirmedDomainEventHandler`):** aggregate raise `HubDiscrepancyRecordedDomainEvent : IDomainEvent` (record) trong factory `Create()`; sau `SaveChangesAsync` interceptor tự publish post-commit; `HubDiscrepancyRecordedDomainEventHandler : INotificationHandler<HubDiscrepancyRecordedDomainEvent>` (Hub.Application/EventHandlers) → `IPublisher.Publish(new HubDiscrepancyRecordedIntegrationEvent(...))`. KHÔNG publish integration event thẳng trong command handler.
- **Atomic (mẫu `ScanInboundCommandHandler`):** mutate xong gọi **1 `SaveChangesAsync`** (mọi repo chung AppDbContext); domain event dispatch tự động post-commit.
- **`HubInboundItem` KHÔNG có `OrderItemId`** (record `(MarketProductId, ProductId?, QuantityKg)`) → discrepancy KHÔNG suy được orderItemId từ inbound event; orderItemId đến từ REQUEST. `order_id` phải RESOLVE qua seam (DEC-HUB-15).
- **`ICreditService.RefundAsync(Guid restaurantId, Guid orderId, decimal amount, string? note, CancellationToken ct)` → `Result<RestaurantCreditDto>`** — chữ ký VERIFY chưa đổi (2026-07-10). Guard `amount ≤ OutstandingBalance` (vượt → `CREDIT_REFUND_EXCEEDS_BALANCE`) ⇒ Orders handler PHẢI clamp.

**Domain:**
- `HubDiscrepancy : AggregateRoot`. String consts `ConditionMissing/Damaged/Partial`, `StatusOpen/Acknowledged`. Factory `Create(hubId, inboundEventId, orderId, orderItemId, affectedQuantity, conditionStatus, notes)` → guard affectedQuantity>0, conditionStatus ∈ {MISSING,DAMAGED,PARTIAL}, ids non-empty; Status=OPEN; `RaiseDomainEvent(new HubDiscrepancyRecordedDomainEvent(Id, HubId, InboundEventId, OrderId, OrderItemId, AffectedQuantity, ConditionStatus, DateTime.UtcNow))`. Method `Acknowledge(Guid adminUserId)` guard OPEN→ACKNOWLEDGED (set AcknowledgedBy/AcknowledgedAt/UpdatedAt), đã ack→InvalidOperation (409).
- Fields: `Id, HubId, InboundEventId, OrderId, OrderItemId, AffectedQuantity(decimal), ConditionStatus(string), Notes?, Status(string=OPEN), AcknowledgedBy?, AcknowledgedAt?, +BaseEntity timestamps`. **KHÔNG lưu restaurant_id** (Orders resolve khi refund/notify).

**Application (Hub):**
- `RecordDiscrepancyCommand(hubId, inboundId, orderItemId, affectedQuantity, conditionStatus, notes)` +Validator +Handler: verify inbound tồn tại + thuộc hub (404); **resolve orderItemId→orderId qua seam `IOrderLookupReader` (DEC-HUB-15), không thấy → 404 `ORDER_ITEM_NOT_FOUND`**; `HubDiscrepancy.Create(...)`; AddAsync; **1 SaveChangesAsync** (→ dispatch domain event); 201 + DiscrepancyDto.
- `AcknowledgeDiscrepancyCommand(hubId, discrepancyId)` +Handler (admin): load (404), `Acknowledge(adminUserId)`, save, 200; đã ack→409.
- `ListDiscrepanciesQuery(hubId, status?)` → cursor paged (private cursor record per repo).
- `HubDiscrepancyRecordedDomainEventHandler` (Hub.Application/EventHandlers) → publish integration event.
- `IHubDiscrepancyRepository`; `IHubDiscrepancyReader.HasOpenDiscrepanciesForOrderAsync(orderId)` (seam Hub EXPOSE cho DEL — DEC-HUB-07; query hub_discrepancies Status=OPEN theo order_id). Đăng ký DI.

**Contracts (2 event mới — DEC-HUB-09):**
- `HubDiscrepancyRecordedIntegrationEvent(Guid DiscrepancyId, Guid HubId, Guid InboundEventId, Guid OrderId, Guid OrderItemId, decimal AffectedQuantity, string ConditionStatus, DateTime OccurredAt) : INotification`.
- `RestaurantRefundIssuedIntegrationEvent(Guid RestaurantId, Guid OrderId, string OrderItemName, decimal AffectedQuantity, decimal RefundAmount, DateTime OccurredAt) : INotification`.

**Orders side (module Orders, TRONG task này):**
- `HubDiscrepancyRecordedIntegrationEventHandler : INotificationHandler<HubDiscrepancyRecordedIntegrationEvent>` (Orders.Application/EventHandlers): load `OrderItem` theo OrderItemId → `LockedUnitPrice` (null/không thấy → log-skip, không refund); load `Order`→`RestaurantId`; `desired = AffectedQuantity × LockedUnitPrice`; **clamp**: `ICreditRepository.FindAccountAsync(restaurantId)` → `outstanding = account?.OutstandingBalance ?? 0`; `refundAmount = Math.Min(desired, outstanding)`; nếu `refundAmount > 0` → `ICreditService.RefundAsync(restaurantId, orderId, refundAmount, "Hub discrepancy {DiscrepancyId}")`. Publish `RestaurantRefundIssuedIntegrationEvent(restaurantId, orderId, OrderItem.ProductNameSnapshot, AffectedQuantity, refundAmount, now)`. **Idempotency MVP:** 1 refund/discrepancy (dispatch 1 lần, không outbox); ghi DiscrepancyId vào note; dedupe-table DEFER. **KHÔNG payment gateway** (DEC-HUB-06). Phần dư (desired>outstanding) bỏ ở MVP (ghi rõ).

**Notifications side (module Notifications, TRONG task này):**
- `RestaurantRefundIssuedIntegrationEventHandler : INotificationHandler<RestaurantRefundIssuedIntegrationEvent>` (mẫu `OrderConfirmedIntegrationEventHandler`): `ResolveUserIdByRestaurantIdAsync` (null→log-skip); `INotificationWriter.WriteAsync(userId, type, title, body, payload{order_id,item_name,affected_quantity,refund_amount,occurred_at})`. SignalR live DEFER (persist DB — DEC-HUB-08).

**Infrastructure (Hub):**
- `HubDiscrepancyConfiguration` snake_case (mirror HubInboundEventConfiguration: ToTable+HasCheckConstraint `condition_status IN ('MISSING','DAMAGED','PARTIAL')`, `status IN ('OPEN','ACKNOWLEDGED')`, `affected_quantity > 0`; index idx_<>). FK nội bộ `inbound_event_id`→`hub_inbound_events`; `order_id`/`order_item_id` = Guid + index (KHÔNG FK cross-module).
- **Seam `IOrderLookupReader` (DEC-HUB-15, Hub→Orders read đầu tiên):** `OrderLookupRow{Guid OrderItemId; Guid OrderId;}` + config `HasNoKey()` + `ToSqlQuery` (SELECT oi."Id" AS "OrderItemId", oi."OrderId" AS "OrderId" FROM order_items oi) — **PascalCase columns** (order_items dùng EF default; xem memory correction project_ef_column_casing) + `OrderLookupReader(AppDbContext db)`. Đăng ký DI. KHÔNG resolve lockedUnitPrice/restaurant ở Hub (Orders lo).
- Migration `AddHubDiscrepancies` (create-only, no-drift, không apply DB).
- **ErrorExtensions:** `ORDER_ITEM_NOT_FOUND`→404 (kết thúc `_NOT_FOUND` → tự map, verify); nếu dùng mã riêng cho acknowledge-đã-ack thì thêm `HUB_DISCREPANCY_ALREADY_ACKNOWLEDGED`→409.

**API:**
- `POST /api/v1/hubs/{hubId}/inbound/{inboundId}/discrepancy` (201) RBAC `hub_staff,admin,operations_manager`
- `GET /api/v1/hubs/{hubId}/discrepancies?status=OPEN` (paged)
- `POST /api/v1/hubs/{hubId}/discrepancies/{id}/acknowledge` (200) RBAC `admin,operations_manager`

**Test ≥80%:** record→201 + assert DomainEvents chứa HubDiscrepancyRecordedDomainEvent; validation conditionStatus/affectedQty; orderItem không tồn tại→404; acknowledge OPEN→ACKNOWLEDGED, đã ack→409; list by status; Orders handler refund=affectedQty×lockedUnitPrice clamp ≤ outstanding + gọi `ICreditService.RefundAsync` (mock; assert KHÔNG gateway) + lockedUnitPrice null→skip + publish RestaurantRefundIssued; Notifications handler persist đúng payload; `HasOpenDiscrepanciesForOrderAsync` (integration — ToSqlQuery không chạy InMemory); wiring domain→integration (integration test nếu khả thi).


### SCRUM-292 — Cross-dock & Prepare Dispatch (UC-HUB-08+09+10) — *phụ thuộc 288*
**Mục tiêu:** cross-dock link inbound→outbound route + outbound tracking. FR-HUB-002, FR-HUB-004.
- **Domain:** `CrossDockTransfer` aggregate (`Id, HubId, InboundEventId (FK), OutboundRouteId, Status(pending/in_progress/completed) default pending, Notes?, timestamps, DeletedAt`). `HubOutboundEvent` aggregate (`Id, HubId, DestinationRouteId, Items(JSONB), TotalQuantityKg, DispatchedAt, RecordedBy?, timestamps`). `Hub.ApplyOutbound(totalKg)` giảm occupied. Inventory `quantity_out += qty` guard ≤ quantity_in.
- **Application:** `CreateCrossDockCommand(hubId, inboundDeliveryId, outboundRouteId)`+Handler (guard inbound status = `ARRIVED_AT_HUB`→ else 422; outbound route cùng hub→ else 422; →201 crossDockId). `RecordOutboundCommand(hubId, destinationRouteId, items[], dispatchedAt)`+Handler (atomic: outbound event + inventory quantity_out += + occupied −=; over-dispatch→422 `INSUFFICIENT_HUB_STOCK`). `ListCrossDockQuery(hubId)` (status). Readers: `IDeliveryRouteReader` (seam Logistics — verify route thuộc hub / tồn tại). `ICrossDockRepository`, `IHubOutboundRepository`.
- **Infrastructure:** configs snake_case; seam `DeliveryRouteRow` (SQL từ `delivery_routes` — snake_case vì Logistics dùng snake_case! verify: quote theo casing thật của Logistics config). Migration `AddCrossDockAndOutbound`.
- **API:** `POST /api/v1/hubs/{hubId}/cross-dock` (201), `GET /api/v1/hubs/{hubId}/cross-dock` (list+status), `POST /api/v1/hubs/{hubId}/outbound` (201+outboundId), `GET /api/v1/hubs/{hubId}/outbound?date=`. RBAC `hub_staff,admin,operations_manager`.
- **Test ≥80%:** cross-dock happy→201; inbound sai status→422; route khác hub→422; outbound happy→inventory quantity_out tăng+occupied giảm; over-dispatch→422 `INSUFFICIENT_HUB_STOCK`; list cross-dock/outbound; concurrency.
- **Status inbound (CHỐT — không còn là điểm mờ):** 288 đã thêm field thật `HubInboundEvent.Status` (PENDING→ARRIVED_AT_HUB). Cross-dock guard ở 292 dùng **đúng giá trị `ARRIVED_AT_HUB`**: `CreateCrossDockCommand` chỉ cho phép khi `inbound.Status == ARRIVED_AT_HUB` (tức đã scan-confirmed), else 422. KHÔNG còn coi "mọi inbound = ARRIVED_AT_HUB" như đề xuất tạm trước đây.

### SCRUM-294 — Handover Goods & Driver Checkout (UC-HUB-11+12) — *phụ thuộc 292; điểm tích hợp Logistics*
**Mục tiêu:** ghi nhận bàn giao goods tại hub cho driver + driver checkout xác nhận. DEC-HUB-11 (không FR riêng).
- **Domain:** `HubHandoverEvent` aggregate (DEC-HUB-11 schema). `Create(...)` (hub staff bàn giao); `ConfirmCheckout(driverUserId)` guard PENDING_CHECKOUT→CHECKED_OUT + verify driver khớp.
- **Application:** `CreateHandoverCommand(hubId, deliveryRouteId, driverUserId, outboundEventId?)`+Handler (verify route+driver qua seam Logistics; verify KHÔNG có discrepancy mở cho các order của route → 409 `PENDING_HUB_DISCREPANCY` dùng `IHubDiscrepancyReader`; →201). `DriverCheckoutCommand(hubId, handoverId, driverUserId)`+Handler (driver xác nhận →200; verify driverUserId = route.DriverUserId chống IDOR). `ListHandoversQuery(hubId)`. `IHubHandoverRepository`, `IDeliveryRouteReader` (tái dùng từ 292).
- **Infrastructure:** config snake_case. Migration `AddHubHandoverEvents`.
- **API:** `POST /api/v1/hubs/{hubId}/handover` (201), `POST /api/v1/hubs/{hubId}/handover/{id}/checkout` (200), `GET /api/v1/hubs/{hubId}/handovers`. RBAC: create=`hub_staff,admin,operations_manager`; checkout=`driver` (chỉ route của mình) + `hub_staff,admin`.
- **Test ≥80%:** handover happy→201; route có discrepancy mở→409 `PENDING_HUB_DISCREPANCY`; driver mismatch→403/422; checkout PENDING→CHECKED_OUT; checkout bởi driver khác→403; list.
- **⚠️ Flag supervisor:** 294 sát DEL epic (SCRUM-299 driver execution). Nếu supervisor muốn dồn handover/checkout vào DEL, có thể DEFER 294 khỏi HUB epic. Leader đề xuất giữ ở HUB vì self-contained (chỉ ghi nhận tại hub, không đụng Order→Delivering).

### SCRUM-296 — Hub Inbound / Outbound History (UC-HUB-13) — *làm cuối*
**Mục tiêu:** query tổng hợp lịch sử inbound/outbound (+ cross-dock/handover) cho hub, filter theo ngày/loại. FR-HUB-003 AC3 / FR-HUB-004 AC3 (tổng hợp).
- **Application:** `GetHubHistoryQuery(hubId, from?, to?, type?)` → trả timeline inbound + outbound (+ optional cross-dock/handover) với totals; cursor pagination (mẫu Pricing `GetPriceChangeHistory` — base64 cursor). Tái dùng repos đã có (inbound/outbound/crossdock/handover). DTO record snake_case.
- **Infrastructure:** query đọc các bảng Hub đã có; nếu cần aggregate phức tạp → `FromSqlRaw` parameterized (KHÔNG string-interpolation). KHÔNG migration mới (trừ index hỗ trợ filter theo ngày nếu thiếu).
- **API:** `GET /api/v1/hubs/{hubId}/history?from=&to=&type=inbound|outbound` (paged). RBAC `admin,operations_manager,hub_staff`.
- **Test ≥80%:** filter theo ngày, theo type, pagination cursor, totals đúng, hub không tồn tại→404, RBAC.

---

## 5. Thứ tự phụ thuộc & lý do
```
286 (bootstrap: Hub aggregate + module wiring)
 └─> 288 (inbound + inventory; cần Hub tồn tại + capacity)
      ├─> 290 (discrepancy cần inbound_event_id; refund/notify cross-module)
      └─> 292 (cross-dock cần inbound ARRIVED_AT_HUB; outbound cần inventory)
             └─> 294 (handover cần outbound + discrepancy-reader từ 290 + route Logistics)
                    └─> 296 (history: query tổng hợp mọi bảng, làm cuối)
```

---

## 6. Nhật ký khảo sát & quyết định
- **2026-07-10** — Khảo sát xong. Xác nhận module Hub rỗng; bootstrap = `EfAssemblyRegistry` + `ForceLoadModuleAssemblies` + Program.cs + test project (mẫu Logistics). Chốt DEC-HUB-01…14. Điểm quan trọng nhất: **DEC-HUB-06** (refund = credit adjustment qua `ICreditService.RefundAsync`, KHÔNG payment gateway/refunds table — override FR-HUB-NEW-003 theo mô hình B2B công nợ thật). **DEC-HUB-07/08** defer enforcement block-DELIVERING (sang DEL) + realtime SignalR (DB-persist). **DEC-HUB-11** Handover = bảng mới self-contained, flag supervisor cân nhắc dồn vào DEL. Sẵn sàng breakdown codex bắt đầu SCRUM-286.
- **2026-07-10 (chốt cuối supervisor)** — 2 điểm mở đã được supervisor XÁC NHẬN: (1) **SCRUM-294 Handover GIỮ trong HUB epic** (không dồn vào DEL) — DEC-HUB-11 chốt, bỏ trạng thái "flag". (2) **MVP coi inbound đã record = `ARRIVED_AT_HUB`** (không thêm status field riêng, giữ field cho tương lai) — chốt phụ ở task 292. DEC-HUB-06 (refund=credit) được supervisor xác nhận đúng. Supervisor tự viết codex prompt SCRUM-286 từ breakdown §4. Reviewer online, review sau khi codex xong 286.
- **2026-07-10 (SCRUM-286 PASS)** — Reviewer PASS độc lập: build 0 lỗi, 47/47 Hub, chạy TOÀN BỘ suite (Auth 370/Catalog 145/Pricing 264/Logistics 279/Orders 463/Notifications 111/Assistant 64/Media 7/Integration 120) 0 regression, format sạch, no-drift, coverage Hub 91.6%. Có test khẳng định no-cross-module-FK + snake_case + RBAC + Ignore(AvailableCapacityKg). 2 finding non-blocking: (LOW) `ListHubsQueryValidator` chưa có test → fold vào 288 hoặc patch nhỏ; (INFO) `UpdateHubCommand` cố ý loại `ManagedBy` khỏi PATCH — LỆCH nhẹ FR-HUB-001 AC3 ('update any field except hubId') → CẦN supervisor chốt: honor AC3 (thêm ManagedBy vào Update) hay giữ reassignment là op riêng. Đã báo supervisor.

## 7. Đối chiếu code THẬT 286 → điều chỉnh cho SCRUM-288 (2026-07-10)
Đọc code codex tạo ở 286. Xác nhận + điều chỉnh so với §4 SCRUM-288:
- **`IHubRepository.HasPendingInboundAsync(Guid hubId, CancellationToken ct)` ĐÃ TỒN TẠI** (stub `return Task.FromResult(false)` + `// TODO(SCRUM-288)`). 288 chỉ thay THÂN hàm (query `hub_inbound_events` pending), **KHÔNG đổi chữ ký** → `DeactivateHubCommandHandler` (đã gọi nó) không phải sửa. AC4 `HUB_HAS_PENDING_DELIVERIES` đã map **409** sẵn trong `ErrorExtensions` → 288 chỉ cần logic thật.
- **Error→HTTP map bằng CODE STRING** ở `src/FreshFlow.API/Extensions/ErrorExtensions.cs` (KHÔNG theo Error type). **288 BẮT BUỘC đăng ký code mới** nếu không sẽ rơi về 500: `HUB_CAPACITY_EXCEEDED`→422 (thêm vào nhánh UnprocessableEntity), `ALREADY_RECEIVED`→409 (nhánh Conflict), `SCAN_NO_MATCH`→404 (**KHÔNG tự map vì không kết thúc `_NOT_FOUND`** → thêm nhánh NotFound tường minh HOẶC đặt code kết thúc `_NOT_FOUND`). (`INSUFFICIENT_HUB_STOCK`→422 để dành 292.)
- **DTO = record property PascalCase** (như `HubDto(Guid HubId, decimal CapacityKg, ...)`) — JSON snake/camel do global config, KHÔNG đặt tên property snake_case. Sửa cách diễn đạt "DTO snake_case" ở §4 thành "record PascalCase, JSON casing global".
- **Cursor pagination = private record trong TỪNG repository** (mẫu `HubRepository.HubCursor`: base64 JSON camelCase `{id,createdAt}`, `OrderByDescending(CreatedAt).ThenByDescending(Id)`, `Take(pageSize+1)`). 288 (pending-inbound, inbound history) định nghĩa cursor record RIÊNG mỗi repo cùng style, KHÔNG tái dùng `HubCursor` (private). Query param `[FromQuery(Name="page_size")]` default 50, validator `InclusiveBetween(1,200)`.
- **Style entity module Hub = plain `sealed class`** (private setters, `Create` factory, `Guid.NewGuid()`/`DateTime.UtcNow` thủ công, `Ignore` computed prop) — **KHÔNG dùng `BaseEntity`** (khác Orders). `HubInboundEvent`/`HubInventory` phải theo ĐÚNG style Hub, không mirror Orders.
- **Capacity guard theo Result pattern:** business rule `HUB_CAPACITY_EXCEEDED` kiểm trong **handler** (available < totalKg → `Result.Failure(Error.Validation("HUB_CAPACITY_EXCEEDED", ...))`), domain chỉ có method mutate occupied (vd `Hub.ApplyInbound(totalKg)` cộng occupied) — KHÔNG throw cho business rule (nhất quán guard `Create/Update` chỉ throw cho invariant kỹ thuật).
- **EF config style (mirror `HubConfiguration`):** `ToTable("...", t => t.HasCheckConstraint(...))`, `HasColumnName` snake_case, `HasColumnType("numeric(10,2)")`, `HasDefaultValue`, `Ignore` computed, index đặt tên `idx_<table>_<col>`. `quantity_available` GENERATED = `HasComputedColumnSql("quantity_in - quantity_out", stored: true)`.
- **DI:** repo `internal sealed (AppDbContext db)`, đăng ký `AddScoped<IXxx, Xxx>()` trong `AddHubModule`. Config `Hub:*` chưa có key nào — nếu 288 cần (vd không) thì thêm qua `TryAddSingleton(config)` đã có.
- **2026-07-10 (findings 286 → 288)** — Supervisor chốt cả 2 finding review 286: (1) `ManagedBy` PHẢI cập nhật được qua PATCH (lệch spec AC3, không phải judgment call) → thêm vào UpdateHubCommand; (2) test `ListHubsQueryValidator`. Cả 2 gộp thành PREREQUISITE FIX ở đầu prompt SCRUM-288. Chuẩn review giữ cho các task sau: test no-cross-module-FK (`GetForeignKeys().BeEmpty()`) + RBAC reflection test.

- **2026-07-10 (SCRUM-290 prep, đối chiếu code thật 288)** — Xác nhận: domain event chỉ dispatch cho `AggregateRoot` (interceptor) → `HubDiscrepancy` kế thừa AggregateRoot (entity Hub khác plain). Pattern domain→integration event = mẫu `Order`/`OrderConfirmedDomainEventHandler` (IPublisher.Publish trong INotificationHandler). `ICreditService.RefundAsync` chữ ký chưa đổi → Orders handler clamp refund ≤ outstanding. `HubInboundItem` không có orderItemId → DEC-HUB-15 (seam Hub→Orders resolve orderId, order_items PascalCase). Casing memory đã sửa (order_items/orders = PascalCase default). §4 SCRUM-290 viết lại đầy đủ, gửi supervisor.

- **2026-07-10 (SCRUM-288 PASS + MEDIUM)** — Reviewer PASS: 89/89, full suite 0 regression, cov 89.5%, 3 error code map đúng (test riêng), 2-step atomic + HasPendingInboundAsync verify bằng test EF thật. **1 MEDIUM cần chốt trước 292:** `decimal.ToInt32(QuantityKg)` drift (xem DEC-HUB-04 addendum) — leader đề xuất đổi HubInventory quantity sang decimal(kg). 2 LOW: `GetPendingInboundQueryValidator`/`ListInboundQueryValidator` chưa test (fold blanket vào 292/fix). Báo supervisor.
- **2026-07-10 (rounding CHỐT)** — Supervisor chốt option (a): `HubInventory` quantity → `decimal NUMERIC(12,2)` kg, bỏ `decimal.ToInt32`. Thực thi = standalone patch codex NGAY (286+288 chưa commit) + gộp 2 LOW validator test. reviewer re-review nhanh phần đổi kiểu. 290/292 không đổi. Inventory nay đo kg → DEC-HUB-04 thống nhất đơn vị.
